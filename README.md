# Triangle-Perimeter
This project is a JavaScript triangle perimeter calculator that takes the coordinates of three points and calculates the lengths of each side using the distance formula. It then adds the side lengths together to find the total perimeter of the triangle. The program uses functions, variables, mathematical formulas, and DOM manipulation to display the results interactively.
